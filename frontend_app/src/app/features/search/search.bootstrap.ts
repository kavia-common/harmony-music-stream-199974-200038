import { ApplicationRef } from '@angular/core';
import { Router } from '@angular/router';

/**
 * PUBLIC_INTERFACE
 * Optional bootstrap utility to forward global search events to routed SearchComponent via a route param in future.
 * Currently serves as documentation placeholder for wiring custom events.
 */
export function setupGlobalSearchListener(appRef: ApplicationRef, router: Router) {
  // Note: No-op, present to indicate pattern.
}
